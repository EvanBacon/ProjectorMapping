Canvas canvas = new Canvas();

void setup() {
  size(600, 400); 
  ellipseMode(CENTER);
  strokeWeight(5);
}

void draw() {
  background(255); // Draw A fresh background
  canvas.draw();
} //draw function